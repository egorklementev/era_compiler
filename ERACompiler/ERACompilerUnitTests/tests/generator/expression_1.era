
code
	int a := 10;
	a := a + a; // 20
	a := a - 5; // 15
	a := a * a; // 225
	a := a & a; // 225
	a := a | a; // 225
	a := a ^ a; // 0
	a := a ? a; // 4
	a := a > a; // 0
	a := a < a; // 0
	a := a /= 0; // 0
	a := a = 0; // 1
	a := a * 7; // 7
end
